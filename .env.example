# Amazon Bedrock uses IAM role authentication
# No API key required - configure AWS credentials via:
# - Lambda: IAM Role (automatically configured)
# - Local: ~/.aws/credentials or AWS_PROFILE environment variable
